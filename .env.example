# CrowdSec Configuration Example
# Copy this file to .env and adjust values as needed

# Ban duration for imported IPs (default: 24h)
# Examples: 1h, 12h, 24h, 7d, 30d
BAN_DURATION=24h

# Reason shown in CrowdSec decisions (default: general-ip-blocklist)
BAN_REASON=general-ip-blocklist

# If CrowdSec runs in Docker, specify the container name
# Leave empty if using local installation
CROWDSEC_DOCKER_CONTAINER=

# Example for Docker:
# CROWDSEC_DOCKER_CONTAINER=crowdsec